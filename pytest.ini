[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --cov=src/recursive_field_math
    --cov-report=term-missing
    --cov-report=xml
    --cov-report=html
pythonpath = src .
